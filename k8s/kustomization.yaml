apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-service

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - qdrant-statefulset.yaml
  - qdrant-service.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
